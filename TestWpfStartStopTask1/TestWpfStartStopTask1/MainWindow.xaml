<Window x:Class="TestWpfStartStopTask1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestWpfStartStopTask1"
        mc:Ignorable="d"
        Title="TestWpfStartStopTask1" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="{Binding Text}" Width="120" Command="{Binding PlayCommand}"/>
        </StackPanel>
        <ListBox Grid.Row="1" ItemsSource="{Binding Items}"/>
    </Grid>
</Window>
