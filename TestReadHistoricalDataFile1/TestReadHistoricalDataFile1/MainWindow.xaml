<Window x:Class="TestReadHistoricalDataFile1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestReadHistoricalDataFile1"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
    <Window.DataContext>
        <local:ViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <!--<Button x:Name="button1" Content="Load" Width="120" Command="{Binding LoadCommand}"/>-->
            <Button Content="Plot" Width="120" Command="{Binding PlotCommand}"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <ItemsControl Grid.Column="0" ItemsSource="{Binding Checkboxes}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox IsChecked="{Binding Checked, Mode=TwoWay}" BorderThickness="0" Content="{Binding Label}" Foreground="{Binding Color}" Background="{Binding Color}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
            <lvc:CartesianChart Grid.Column="1" Grid.Row="1" Series="{Binding Series}" AxisY="{Binding YAxes}" AnimationsSpeed="0:0:0.5" Hoverable="False" 
                                DisableAnimations="True" ToolTip="{x:Null}" Zoom="X">
                <lvc:CartesianChart.CacheMode>
                    <BitmapCache EnableClearType="False" RenderAtScale="1" SnapsToDevicePixels="False"/>
                </lvc:CartesianChart.CacheMode>
                <lvc:CartesianChart.DataTooltip>
                    <lvc:DefaultTooltip BulletSize="20" Background="{DynamicResource C5}" IsWrapped="False"/>
                </lvc:CartesianChart.DataTooltip>
                <lvc:CartesianChart.AxisX>
                    <lvc:Axis LabelFormatter="{Binding Formatter}" Unit="{Binding AxisUnit}" DisableAnimations="True" LabelsRotation="0">
                        <lvc:Axis.Separator>
                            <lvc:Separator Step="{Binding AxisStep}" StrokeThickness="0"/>
                        </lvc:Axis.Separator>
                    </lvc:Axis>
                </lvc:CartesianChart.AxisX>
            </lvc:CartesianChart>
            
        </Grid>
        
    </Grid>
</Window>
