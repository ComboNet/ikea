<Window x:Class="TestWpfFadingNotification1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestWpfFadingNotification1"
        mc:Ignorable="d"
        Title="TestWpfFadingNotification1" Height="450" Width="800">
    <Window.DataContext>
        <local:ViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="True" Width="120" Command="{Binding TrueCommand}"/>
            <Button Content="False" Width="120" Command="{Binding FalseCommand}"/>
        </StackPanel>
        <Label Grid.Row="1" Content="Submitted!" Height="29" HorizontalAlignment="Left" HorizontalContentAlignment="Center" Margin="126,67,0,0" Opacity="0.0" VerticalAlignment="Top" Width="251" Background="#FF76FF04">
            <Label.Style>
                <Style>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsVisible}" Value="True">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="(Label.Opacity)" From="1.0" To="0.0" Duration="0:0:2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Label.Style>
        </Label>
    </Grid>
    
    
    
    
    <!--<Grid>
        <StackPanel>
            <Label Content="Submitted!" Height="29" HorizontalAlignment="Left" HorizontalContentAlignment="Center" Margin="126,67,0,0" Name="LabelTicketSubmitted" Opacity="0.0" VerticalAlignment="Top" Width="251" Background="#FF76FF04" />
            <Button x:Name="ButtonSubmit" Content="Button" HorizontalAlignment="Left" Margin="214,101,0,0" VerticalAlignment="Top" Width="75"  />
            <Label Content="Submitted!" Height="29" HorizontalAlignment="Left" HorizontalContentAlignment="Center" Margin="126,67,0,0" x:Name="LabelTicketSubmittedCopy" Opacity="0.0" VerticalAlignment="Top" Width="251" Background="#FF76FF04" />
            <StackPanel.Triggers>
                <EventTrigger SourceName="ButtonSubmit" RoutedEvent="Button.Click" >
                    <BeginStoryboard Name="FilterStoryboardRectangleTicket">
                        <Storyboard>
                            <DoubleAnimation
                                          Storyboard.TargetName="LabelTicketSubmitted" 
                                          Storyboard.TargetProperty="(Label.Opacity)"
                                          From="1.0" To="0.0" Duration="0:0:2" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </StackPanel.Triggers>
        </StackPanel>
    </Grid>-->
</Window>
